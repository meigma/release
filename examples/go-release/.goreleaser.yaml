# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: example

gomod:
  proxy: true
  env:
    - GOTOOLCHAIN=local
    - GOPROXY=https://proxy.golang.org,direct
    - GOSUMDB=sum.golang.org

builds:
  - id: example
    main: ./cmd/example
    binary: example
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -buildid=
      - -X main.version={{ .Version }}
      - -X main.commit={{ .FullCommit }}
    mod_timestamp: "{{ .CommitTimestamp }}"

notarize:
  macos:
    - enabled: '{{ eq .Env.MACOS_NOTARIZE_ENABLED "true" }}'
      ids:
        - example
      sign:
        certificate: "{{ .Env.MACOS_SIGN_P12 }}"
        password: "{{ .Env.MACOS_SIGN_PASSWORD }}"
      notarize:
        issuer_id: "{{ .Env.MACOS_NOTARY_ISSUER_ID }}"
        key_id: "{{ .Env.MACOS_NOTARY_KEY_ID }}"
        key: "{{ .Env.MACOS_NOTARY_KEY }}"
        wait: true
        timeout: 20m

archives:
  - id: example
    ids:
      - example
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}

nfpms:
  - id: release
    ids:
      - example
    vendor: Example Organization
    homepage: https://github.com/OWNER/REPOSITORY
    maintainer: Release Team <releases@example.com>
    description: Example Go command using the reusable release workflows.
    license: Apache-2.0 OR MIT
    formats:
      - deb
      - rpm
      - apk
    bindir: /usr/bin
    rpm:
      signature:
        key_file: "{{ .Env.RELEASE_RPM_SIGNING_KEY_FILE }}"
    apk:
      signature:
        key_file: "{{ .Env.RELEASE_APK_SIGNING_KEY_FILE }}"
        key_name: example-001

homebrew_casks:
  - name: example
    ids:
      - example
    binaries:
      - example
    repository:
      owner: HOMEBREW-OWNER
      name: HOMEBREW-TAP
    homepage: https://github.com/OWNER/REPOSITORY
    description: Example Go command
    license: Apache-2.0 OR MIT
    url:
      template: "https://github.com/OWNER/REPOSITORY/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    skip_upload: true

scoops:
  - name: example
    ids:
      - example
    repository:
      owner: SCOOP-OWNER
      name: SCOOP-BUCKET
    homepage: https://github.com/OWNER/REPOSITORY
    description: Example Go command
    license: Apache-2.0 OR MIT
    url_template: "https://github.com/OWNER/REPOSITORY/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    skip_upload: true

checksum:
  name_template: checksums.txt

sboms:
  - id: archives
    artifacts: archive
  - id: packages
    artifacts: package

signs:
  - id: checksums
    cmd: cosign
    artifacts: checksum
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - --yes

changelog:
  disable: true

release:
  disable: true
