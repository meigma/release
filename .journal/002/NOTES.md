---
id: 002
title: Session start (goal pending)
started: 2026-08-18
---

## 2026-08-18 12:35 — Kickoff
Goal for the session: not yet stated. The user asked to start a new session without naming a task; the concrete goal will be appended when they state their actual request.
Current state of the world: `main` is at squash commit `5566640c061c5e36f3715e0a1b57eaf69646a0ba` (PR #2), which carries the rehearsed GitHub Release and OCI delivery MVP. `journal/jmgilman` is at `b452dba` (`docs(journal): close session 001`) and in sync with `origin`. Session 001 is closed and complete.
Open threads inherited from 001: Homebrew, MacPorts, Nix, Scoop, generalized installer, and mise channels are unimplemented; Release Please PR #3 (`chore(main): release 0.1.0`) is unmerged; documented reusable-workflow pins still point at pre-squash revision `fb8c8098ff27968fb3070e928c00e925f38c698e`; disposable rehearsal repo `meigma/release-oci-remediation-e2e` awaits manual deletion.
Plan: wait for the user's request, then work incrementally in an implementation worktree created from fetched `main`, integrating via GitHub PR squash merge.

## 2026-08-18 12:41 — Adopt template-go rules in AGENTS.md
Goal (now stated): pull the rules from `~/code/meigma/template-go/AGENTS.md` into this repo's `AGENTS.md`.
Findings: our `AGENTS.md` held only the `ai-protocol` block, identical to the template's. `CLAUDE.md` is a symlink to `AGENTS.md`, so it inherits any change automatically.
Done: worktree `docs/agents-go-rules` off `main`; appended the template's `# Go Best Practices` section (A/T/R/P/D/I/E/L rule groups) verbatim. `diff AGENTS.md ~/code/meigma/template-go/AGENTS.md` reports identical. Commit `docs(agents): adopt template-go engineering rules`, PR https://github.com/meigma/release/pull/4.
Gotcha: `read` of the template showed 97 lines while the file has 98; the first `sed -n '14,97p'` silently dropped the final rule line. Verified with `diff`, then reappended with `sed -n '14,$p'`.
Next: PR #4 awaits review/squash merge. Existing Go code in `cmd/release-mvp` and `internal/cli` has not been audited against the newly adopted rules (notably D1/D4 Godoc and `doc.go`).

## 2026-08-18 12:45 — PR #4 merged
Merged PR #4 as squash commit `0c39bedb9551031b2d704993397a2c2ce0a3769b` on `main`; `AGENTS.md` on `main` verified byte-identical to the template's.
Gotcha: `gh pr merge --squash --delete-branch` merged server-side but then failed locally with `fatal: 'main' is already used by worktree at ...` (gh tried to check out `main` inside the branch worktree, which Worktrunk already holds in the primary checkout). Consequence: neither the remote nor local branch was cleaned up by gh. Recovered with `git fetch --prune`, `git pull --ff-only` in the main checkout, `wt remove docs/agents-go-rules`, and `git push origin --delete docs/agents-go-rules`.
Takeaway for future merges in this repo: run `gh pr merge` from the main checkout, or pass `--delete-branch` only after leaving the PR worktree; expect to prune the branch manually otherwise.
Next: still open from session 001 — deferred package channels, release PR #3, doc pins on pre-squash SHA, rehearsal repo deletion. Newly adopted A/T/R/P/D/I/E/L rules have not been applied to existing Go code.

## 2026-08-18 12:52 — New goal: release CLI architecture
Stated goal: design a profile-driven Go CLI that replaces ~90% of the bespoke logic in `go-pre-publish.yml` (145 lines), `go-oci-build.yml` (445), `publish-github-release.yml` (482), and `publish-oci-image.yml` (569) — ~1650 lines total.
Requirements captured from the user: profile flag rather than per-language commands (`release-cli prepublish --profile go`); hexagonal architecture with all I/O behind ports and strong domain typing per AGENTS.md; locally testable without CI; this repo dogfoods the CLI (CI builds the branch binary, consumers install a released version, workflows branch between the two); portable across at least four GitHub orgs with only credential-level changes (no hardcoded `meigma`); weigh custom action vs reusable workflow from the consumer's perspective against GitHub's documented limits; base the CLI on `~/code/meigma/template-go`.
Approach: three parallel grounding researchers (workflow capability inventory, Actions packaging constraints, template-go conventions) -> software-architect -> adversarial architecture review -> final doc in `.journal/002/`.

## 2026-08-18 13:32 — Architecture draft + adversarial review landed in journal
Pipeline run: three parallel grounding researchers -> software-architect (rev 1, 313 lines) -> architecture-reviewer (276-line punch list, verdict "approve with changes") -> architect revision 2 (335 lines, §13 dispositions table).
Deliverables in `.journal/002/`: `ARCHITECTURE.md` (revision 2, for review), `ARCHITECTURE-REVIEW.md`, and `research/{workflow-inventory,actions-constraints,template-conventions}.md` as cited ground truth.
Shape settled in rev 2: `release-cli` with ecosystem-neutral verbs (`stage`, `image build|verify`, `publish oci prepare|finalize`, `publish github`, `plan tags`, `verify handoff|bundle`, `config`), `--profile` only on `stage`; pure core `internal/rel`; narrow consumer-declared ports with one concrete adapter each; profiles as compiled-in Go interfaces emitting one logical release manifest with stage-scoped projections copied into today's two disjoint artifacts.
Key review-driven corrections: OCI publication split into `prepare` -> YAML `actions/attest` -> `finalize` so invariant 14 (trust metadata before public tags) stays enforceable; narrow FS ports (`BundleScanner`, `ArtifactsReader`, `manifest.Store`) instead of I/O in engines; `ChecksumSet` (claim) vs scanner-reconciled `Bundle` (fact); service-surface ports decomposed; "safe to re-run" dropped from exit 3 in favor of a `mutations` status plus per-command postcondition reconciliation; `$/` skew claim demoted to a stamped protocol-integer handshake with the distribution repo derived from `github.action_repository`; `actions/create-github-app-token` stays in YAML; `gh release upload --clobber` retained behind an `AssetReplacer` port.
CI delivery recommendation: layered reusable workflows -> composite `setup-release-cli` -> CLI, because job-scoped permissions/environments/concurrency/needs/matrices/outputs exist only in reusable workflows while a composite is the only same-job CLI bridge. Dogfood via explicit `cli-path` from a branch build; consumers get a stamped default version plus protocol check.
Slice 1 (one PR): `internal/rel`, `profile`+`goprof` selection, `stage/assets` engine, `bundlefs`/`manfs`/`actenv` adapters + mocks, `--json` envelope, `stage --profile go` replacing PP-06–PP-08 bash (~80 lines), manifest emission into both uploads, minimal `setup-release-cli`. GoReleaser invocation stays in YAML until slice 6.
Open decisions for the user: approve rev 2 or push back on the projection-vs-combined-artifact call, native `oras-go` vs ORAS binary (scratch-GHCR spike is a slice-3 acceptance gate), and whether attestation creation eventually moves into the CLI.
Next: user reviews `.journal/002/ARCHITECTURE.md`; no code written yet.

## 2026-08-18 14:05 — Complexity review of architecture rev 2
Ran a complexity-reviewer against `ARCHITECTURE.md` rev 2; output in `.journal/002/ARCHITECTURE-COMPLEXITY-REVIEW.md` (383 lines). Verdict: "substantially simplify" — direction proportionate, initial architecture is not.
Counts it produced: rev 2 introduces 26 non-mock packages, 13 mock packages, ~21 port seams, 20 domain types, 10 command leaves, 4 config layers, 5 test modes. Slice 1 alone = 14 namespaces to replace PP-06–PP-08, which is 3 logical checks / 32 lines (`go-pre-publish.yml:88-119`, verified). That is the central finding.
Accounting defects it caught in §11: the six slice line estimates sum to 1460 and with 250–300 residual lines exceed the 1641-line source total (145+445+482+569, verified); slice 2 claims four `getArtifact` blocks when the inventory has three (OB-06/GR-05/OP-03); GR-05 is double-counted in slices 2 and 4. Named migrated operations are 42 of 70, ~64.5% of lines — so the "~90%" claim needs restating as a share of bespoke decision logic, not lines.
Cut now: manifest/projections/store/emission from slice 1 (no reader yet); profile interface+registry+`Name()` (one implementation — keep `--profile go` as direct dispatch); `phase`/`mutations` JSON fields and exit code 3 (nothing mutates yet); `release.toml` + `config show|validate` (evidence-triggered, not slice-numbered); `cli-version` override and therefore the protocol handshake (remove the skew path instead of modeling it); testscript/`e2e` from slice 1.
Collapse now: use `fs.FS`/`io.Reader` instead of custom `ArtifactsReader`/`manifest.Store` ports; merge `Notifier`+`Outputs`; merge `DraftFinder`+`AssetReader` into one read-only port in slice 4; inject `sleep(ctx,d)` instead of an `internal/clock` package; defer `execx` and the catch-all `verify` package.
Its rule-conflict resolution: A1 requires a boundary, not a bespoke boundary — stdlib interfaces satisfy A1 for deterministic local plumbing (R1/R3 win there), while A1/A2 win at remote/subprocess/credential/irreversible-mutation boundaries. A3 applies at use-case boundaries, not per type.
Held as correctly complex: prepare→attest→finalize with fresh-state drift refusal (invariant 14); reusable-workflow ownership of job policy; bootstrap checksum+attestation verification; three-owner handoff integrity; strict closed/signed bundle validation; exact-byte/no-rebuild OCI checks; pure Version/tag/channel planner (slice 3); registry read/push/tag port separation; GitHub replace-vs-publish separation; App token action staying in YAML; mockery/Godoc for the adapters that survive.
Proposed first move: a one-day throwaway prototype — `release-cli stage --profile go --dist ./dist` in four packages (`cmd/release-cli`, `internal/cli`, `internal/stage`, `internal/profile/goprof`) over `os.DirFS`, no ports/mocks/manifest/testscript, validated against real pinned GoReleaser 2.17.1 output plus four deliberate mutations (bad checksum, missing arch record, escaped path, cleared exec bit).
Next: user decides which cuts to accept; rev 3 of `ARCHITECTURE.md` should fold in accepted cuts and fix the §11 burn-down arithmetic. No code yet.

## 2026-08-18 14:41 — Architecture revision 3
Rev 3 replaces `.journal/002/ARCHITECTURE.md` (275 lines, down from rev 2's 335). Rev 2 remains in git history at commit 5afb2e3 if the earlier shape is needed.
Adjudication folded in — accepted cuts: manifest/projections/store out of slice 1 (trigger: first downstream CLI reader of staged facts); profile interface/registry/`Name()` removed in favor of direct `--profile go` dispatch (trigger: second real profile); slice-1 JSON reduced to `{schema,command,ok,result}` with exits 0/1/2 (trigger for mutation status: first ambiguous remote write plus a caller that branches on it); `release.toml` + `config show|validate` made evidence-triggered; testscript/`e2e` dropped from slice 1 in favor of three evidence layers.
Accepted collapses: stdlib `fs.FS`/`io.Reader` boundaries instead of custom local-file ports, with the binding rule resolution stated once — A1 requires a boundary, not a bespoke boundary; A1/A2 win at remote/subprocess/credential/irreversible-mutation boundaries, R1/R3 win for local deterministic plumbing, A3 at use-case boundaries. `DraftFinder`+`AssetReader` merged into `ReleaseReader` (slice 4) while `AssetReplacer` and `Publisher` stay separate; injected `sleep(ctx,d)` instead of `internal/clock`; `execx`, `verify`, `manfs`, `e2e` all dropped until evidence.
Port count fell from ~21 seams to 13 genuine ports, all at remote/subprocess boundaries. Slice 1 is four production packages: `cmd/release-cli`, `internal/cli`, `internal/stage`, `internal/profile/goprof`.
`cli-version` decision (mine, differing from the complexity review's conditional defer): the workflows, composite action, and CLI are ONE release unit with one version and one pin, grounded in `release-please-config.json:11-16` (single root package, `include-component-in-tag: false`). The input is removed permanently as unsupported; `cli-path` stays as a documented unsupported escape hatch covering dogfood, regression bisection, vendored binaries, and air-gapped orgs. A cheap stamped-integer guard is retained: fail closed on the installed path, warn-and-report on the `cli-path` path. Rationale on record — supporting independent pinning would make every YAML<->binary touchpoint a versioned API with a support window plus a workflow-ref x CLI-version CI matrix; and the failure asymmetry matters, since newer-workflow/older-CLI fails loudly at flag parsing while older-workflow/newer-CLI can fail silently when a renamed output reads empty and a gating step proceeds. Revisit trigger: first adopter needing genuinely different cadences, answered then with a supported N-1 window and a real compat matrix.
§11 arithmetic now reconciles and I verified the spans independently: 32 (PP-06–08, `go-pre-publish.yml:88-119`) + 108 (three metadata blocks at 36 lines each — OB-06 `106-141`, GR-05 `129-164`, OP-03 `88-123`) + 307 (OP-09–15 `234-481`, OP-19–20 `511-569`) + 310 (GR-06–12 `166-333`, GR-14–18 `341-482`) + 287 (OB-08–21 `150-436`) + 13 (PP-05 `74-86`) = 1057, plus 584 residual = 1641 exactly. GR-05 counted once. Headline restated: 42 of 70 logical steps, upper bound ~1059/1641 lines (~64.5%); the whole-file "~90%" claim is retired.
Also fixed from my own rev-2 read: `Receipt` renamed `OCIPrepareResult` and carried as the `--json` result re-fed to `finalize` on stdin rather than a file, so no persistence port exists; `--dry-run` on prepare yields a non-authoritative result that `finalize` refuses.
Next: build slice 1, or first run the one-day throwaway prototype (`stage --profile go --dist ./dist`, four packages, no ports/mocks) against real pinned GoReleaser 2.17.1 output with four deliberate-mutation checks.

## 2026-08-18 15:22 — End-to-end implementation plan
`.journal/002/PLAN.md` (1117 lines) implements `ARCHITECTURE.md` rev 3 as 11 PRs across the 6 slices, with a spine table (title, slice, contents, replaced steps, dependency, merge gate) plus per-PR Scope / Files / Interfaces / Behavior-preserved / Tests / Workflow-YAML changes / Verification / Must-NOT-contain / Rollback.
Spot-verified its repo facts: `go.mod` is module `github.com/meigma/release`, Go 1.26.6, Cobra v1.10.2, Testify v1.11.1, Viper absent (plan adds v1.21.0 in PR 1); `moon.yml` project title is still `release-mvp`. Its 42-step accounting table reconciles to exactly the architecture's 42 of 70 migrated operations.
PR order: 1 stage/`--profile go` + `release-mvp`→`release-cli` transition; 2 `verify handoff` + first ports + mockery; 3 `plan tags` + `internal/rel` + `StateReader`; 4 `publish oci prepare` + `ContentPusher`/`Signer`; 5 `publish oci finalize` + workflow cutover (prepare → 3 attest steps → finalize); 6 `verify bundle`; 7 `publish github`; 8 `image build` (this is where the deferred persisted projection trigger legitimately fires, OCI-input side only); 9 `image verify`; 10 GoReleaser invocation moves into `goprof`; 11 docs pin to the released SHA.
Notable calls it made: the artifact rename rides PR 1 atomically rather than a separate rename PR, because a rename-only PR would either introduce `cmd/release-cli` before the four-package slice or keep both binaries, contradicting the clean-cutover rule; `.mockery.yml` deliberately lands in PR 2, not PR 1, since slice 1 has only stdlib boundaries; the port budget is closed at 13 with "no PR may add a fourteenth port".
Three spikes gate work, each with a same-ports fallback: `$/` self-reference under an external SHA-pinned caller (blocks PR 1; fallback is checking out the callee at `github.job_workflow_sha` and using its local composite, with identity derived from `github.job_workflow_ref`, never hardcoded); oras-go v2 GHCR parity incl. referrer coexistence with the three `actions/attest` referrers and failpoint recovery (blocks PR 3; fallback keeps the pinned ORAS binary behind the same three ports); release-please stamping of `action.yml` (blocks the PR 1 release cutover; fallback stamps the semantic version via `extra-files` and guards the protocol integer with a CI equality check).
It also carries the deferred-trigger list as an explicit "do not build early" section, and flags that extracting `internal/execx` would need an architecture amendment since rev 3 prohibits it.
Fixed before filing: four occurrences where the `$/` self-repository token had been mangled to a bare `$`.
Next: execute PR 1, or run spike A ($/ self-reference) first since it gates PR 1's action shape.

## 2026-08-18 15:52 — Spike A executed: $/ self-reference PASSES
Verdict: PASS on all five pass criteria; PR 1 is unblocked and no fallback plumbing is needed. `PLAN.md` §5A now carries the result inline.
Setup: throwaway branch `spike/self-ref` in `meigma/release` with `.github/actions/spike-marker/action.yml` (composite printing a commit-embedded marker plus non-secret action identity) and `.github/workflows/spike-self-ref.yml` (a `workflow_call` shell using `uses: $/.github/actions/spike-marker`, `permissions: {}` at workflow and job level). External caller: scratch public repo `meigma/release-selfref-spike`, pinning the callee by full SHA.
Evidence (runs 32191413160, 32191526616, 32191534462): runner `2.336.0` on `ubuntu-24.04`; log line `Download action repository 'meigma/release@a47ed6648850c73b91d31f794fbd443e6beffff6'`; `github.action_repository=meigma/release` while `github.repository=meigma/release-selfref-spike`; `github.action_ref` equals the pinned full SHA; action path resolved under `_actions/meigma/release/<sha>/.github/actions/spike-marker`; `GITHUB_WORKSPACE` had 0 entries, so no checkout is required to load the action; callee workflow outputs propagated to the caller job.
Pin immunity (the decisive test): moved the branch tip to `c47415f6480fd9101f222b0f717031991601acd2` changing the marker to MARKER-B, then re-dispatched the caller still pinned at `a47ed66…` — it resolved MARKER-A, and the debug lines added in the newer commit did not appear. A second caller pinned at `c47415f…` resolved MARKER-B. So an external SHA pin transitively pins the sibling action, which is the property the one-release-unit contract depends on.
Rerun semantics: a deliberate first-attempt failure in a probe job, then `gh run rerun --failed`; attempt 2 re-resolved the same pinned commit and MARKER-A and went green.
Side finding that corrects the plan: `github.job_workflow_ref` and `github.job_workflow_sha` evaluate to `null` inside the called reusable workflow's expression context, and `github.workflow_ref`/`github.workflow_sha` report the caller's workflow and commit (`.../call-spike-b.yml@refs/heads/main`, `477325db…`), not the callee's. Those two values are OIDC token claims, not usable github-context properties. The plan's original spike-A fallback ("check out the callee at `github.job_workflow_sha`") was therefore not implementable; §5A now specifies an explicit callee-ref input instead, which the caller sets to the same SHA it pins. Unused while `$/` works, but worth knowing before someone reaches for it.
Cleanup: local worktree removed; remote branch `spike/self-ref` deliberately retained as the evidence anchor for the two cited SHAs (deleting it would make them unreachable); scratch consumer `meigma/release-selfref-spike` archived rather than deleted because the current token lacks `delete_repo` — same limitation recorded in session 001 for `meigma/release-oci-remediation-e2e`. Both need manual deletion when the evidence is no longer wanted.
Next: PR 1 per `PLAN.md` §3, gated also on spike C (release-please stamping of `action.yml`); spike B (oras-go GHCR parity) is not needed until PR 3.
