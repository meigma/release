---
id: 001
title: Meigma delivery infrastructure
started: 2026-08-16
---

## 2026-08-16 21:51 — Kickoff
Goal for the session: Establish one opinionated Go delivery process for the Meigma organization covering GitHub Release artifacts, OCI images, Homebrew, MacPorts, Nix, Scoop, a generalized installer script, and mise.
Current state of the world: The public `meigma/release` repository exists, its session protocol is installed on `main`, and the personal `journal/jmgilman` worktree is ready.
Plan: Capture lightweight research and decisions per delivery channel, starting with the shared release contract and GitHub Release artifacts, then formalize the proven process into reusable automation and documentation.

## 2026-08-16 21:59 — Initial automation structure
The working direction is one centrally governed release DAG per producer profile, not independent release-type workflows that each rebuild. A language-specific build job should produce one immutable release bundle containing distributable files, `checksums.txt`, SBOMs, and a machine-readable manifest tied to the tag and commit. Optional publisher jobs then consume and verify that bundle without compiling source.
Reusable workflows should own runner boundaries, matrices, permissions, secrets, publication, and attestations. Composite actions should be reserved for repeated step-level mechanics inside jobs. Repositories should use a thin caller workflow and select supported publishers through explicit inputs.
Likely producer profiles are Go, Python, and later specific IaC deliverable types; IaC is not one coherent build profile. GitHub Releases, OCI packaging, and package-manager metadata can share the same release bundle, but distributed publication cannot be truly atomic. Publishers must therefore be idempotent and the GitHub Release should remain a draft until required publication and attestation stages succeed.
Open issues for channel research: whether MacPorts and Nix should intentionally rebuild from source or consume authoritative binaries; which channels are required versus optional for finalization; and the exact release-bundle manifest schema.

## 2026-08-16 22:11 — Draft release and source package boundaries
GitHub explicitly recommends creating a draft, attaching all assets, and only then publishing when immutable releases are enabled. The candidate workflow should therefore build and validate first, then create or reconcile the exact draft release and upload assets. Initial automation should stop with a reviewable draft rather than automatically crossing the irreversible publication boundary.
Manual publication can become the promotion signal. A later `release.published` workflow may perform retryable distribution work, while candidate-stage automation should avoid irreversible external writes. OCI publication ordering remains an open design choice because publishing it before approval creates a separate irreversible surface, while publishing after approval allows temporary channel skew.
Nix is reclassified as repository scaffolding rather than a release asset: provide and validate a `flake.nix`, let users consume tagged Git source, and do not assume nixpkgs upstreaming.
MacPorts conventionally consumes source through a Portfile and its `golang` PortGroup runs `go build`; MacPorts then creates its own binary archives where applicable. Treat MacPorts as source-package metadata rather than a consumer of Meigma's canonical release binaries. Without upstreaming, distribution would also require a custom ports tree and `PortIndex`.

## 2026-08-16 22:41 — mkosi and OBS interoperability research
Parallel research confirmed that mkosi and Open Build Service are actively interoperated, but primarily for OS images rather than application OCI images. OBS has recognized `Type: mkosi` as a native recipe since 2022, with compatibility work merged in February 2026. ParticleOS is the clearest live example: GitHub/OBS workflows import mkosi configuration, OBS builds offline against its prepared package mirror, and current signed EFI, raw disk, ESP, verity, manifest, and checksum outputs are published from `system:systemd`.
mkosi is principally a distro-aware OS image assembler. Current mkosi supports `Format=oci`, introduced in April 2024, and emits a single-platform, single-layer OCI Image Layout directory with labels and annotations. It neither pushes to registries nor aggregates multi-platform indexes; those require external OCI tooling.
The native OBS mkosi adapter cannot currently publish that OCI format. `build-recipe-mkosi` writes outputs under `OTHER` and deletes directory outputs because OBS cannot publish them. It also lacks the `DOCKER` result directory and `.containerinfo` metadata used by OBS's registry publisher. No live public `Type: mkosi` plus `Format=oci` pipeline was found. Native OBS registry paths are Dockerfile with Podman or KIWI.
OBS does provide draft-like promotion semantics independently of mkosi: build and publish flags are separate, and a manual release target can copy completed sources and binaries without rebuilding into a build-disabled publication project. For containers the target publisher may reconstruct manifests and indexes, so the promoted top-level OCI digest is not documented as identical and must be verified.
Decision direction: mkosi plus OBS is viable for future OS, UKI, sysext, or appliance delivery. It is not the recommended application-OCI path for Meigma today. If OBS is adopted for Go application OCI, use its Dockerfile/Podman or KIWI adapter and manual release targets; otherwise retain a GitHub-centered OCI publisher. The final target registry digest remains the attestation subject.
Primary references: https://openbuildservice.org/help/manuals/obs-user-guide/cha-obs-package-formats#sec-pkgfmt-mkosi, https://github.com/openSUSE/obs-build/blob/master/build-recipe-mkosi, https://github.com/systemd/mkosi/blob/main/mkosi/resources/man/mkosi.1.md#output, and https://github.com/systemd/particleos.

## 2026-08-17 07:50 — apko and melange research
Verdict: apko is a strong candidate for Meigma's daemonless application-image composer, with melange as the necessary adapter that turns the already-built Linux executable into an APK. This preserves the build-once rule if melange only copies the canonical binary into the package and does not compile or strip it. The proposed byte path is `canonical Linux binary -> architecture-specific APK -> apko OCI layout -> registry`; the experiment must compare the executable digest at every boundary.

apko is declarative and has no Dockerfile `RUN` equivalent. It resolves and verifies APK repositories, lays out package contents without running package scripts by default, emits single- or multi-architecture OCI images, generates SPDX image SBOMs, and can either write an OCI Image Layout directory or publish directly through `go-containerregistry`. `apko publish` returns the final digest and can write all published references to a file. A lockfile generated by `apko lock` is required for our reproducibility claim: the YAML alone still resolves against mutable repository indexes, while the lock records exact package versions and checksums.

apko cannot add an arbitrary release binary directly; custom files must first be packaged as APKs. melange supplies that package boundary, can generate and sign a local repository index, creates package SBOMs, supports fixed build timestamps, and can run a minimal pipeline that installs a prebuilt file from `--source-dir`. This is more machinery than a Dockerfile `COPY`, but it gives apko a complete package inventory and avoids an opaque mutation layer. Do not use melange's Go pipelines here: compiling again would violate the authoritative-artifact invariant.

The “non-daemon” description needs qualification. apko itself writes or pushes images without a Docker daemon. melange requires an execution runner for pipeline commands: current choices are Bubblewrap, Docker, and QEMU. Bubblewrap avoids a container daemon, but GitHub's `ubuntu-latest` requires privileged host setup to relax AppArmor's unprivileged-user-namespace restriction. The official setup action uses `sudo` to install an AppArmor profile and reload it. Thus this path is daemonless, not privilege-free.

Supply-chain controls remain the outer workflow's responsibility. apko verifies APK repository signatures unless explicitly disabled and generates SBOM files, but it does not sign the final OCI digest or emit SLSA provenance. melange's opt-in provenance is an in-toto SLSA v1 statement beside each APK and can be APK-key signed; however, it contains the package configuration and final APK digest but no resolved dependencies, and its builder ID is hard-coded to a Chainguard URL even for self-run builds. Treat it as package metadata, not evidence of a Meigma SLSA level. The central GitHub workflow should attest the final OCI digest and image SBOM with GitHub's attestation actions and sign the digest with Cosign if registry consumers require that verification path.

Chainguard coupling is acceptable but real. Both projects are Apache-2.0, actively maintained, and released frequently; as of this check apko was v1.2.37 and melange v0.59.1, with both repositories and Wolfi receiving changes today. The tools can consume Alpine or a Meigma-local APK repository and do not require Chainguard's commercial service. Coupling remains in project governance, Go module paths, public installation images, documentation, APK conventions, and melange's provenance identifiers. melange is still pre-1.0, and apko's README continues to call the project a work in progress. Pin exact tool versions.

Do not adopt the existing convenience actions as the trust boundary. `chainguard-dev/actions/apko-build` declares itself deprecated, while `setup-melange` defaults to the latest release and downloads a tarball through `curl | tar` without verifying its supplied Cosign signature. apko and melange releases provide checksums plus `.sig` and `.crt` assets. A Meigma workflow should download fixed versions and verify their release signatures against the exact upstream GitHub Actions workflow identity before execution.

Candidate direction: build the APK and apko OCI layout during the reversible candidate run, upload the layout, lockfile, and SBOMs as immutable workflow artifacts, and inspect them before release approval. After `release.published`, push that exact layout rather than rebuilding it, resolve the registry digest, and attest it. This avoids publishing a candidate tag before human approval and avoids post-approval recompilation. The next prototype must prove that the selected layout-to-registry tool preserves the apko index digest, that the image executable equals the canonical release binary, and that amd64/arm64 images run correctly.

Primary references: https://github.com/chainguard-dev/apko/blob/main/README.md, https://github.com/chainguard-dev/apko/blob/main/internal/cli/build.go, https://github.com/chainguard-dev/apko/blob/main/internal/cli/publish.go, https://github.com/chainguard-dev/melange/blob/main/README.md, https://github.com/chainguard-dev/melange/blob/main/docs/md/melange_build.md, https://github.com/chainguard-dev/melange/blob/main/pkg/build/slsa.go, and https://github.com/chainguard-dev/melange/blob/main/.github/actions/setup-bubblewrap/action.yaml.

## 2026-08-17 10:00 — Native Linux packages
Add DEB and RPM to the Go delivery contract. They should be candidate-stage derivations of the same canonical Linux executables, built before the draft release is published. Use GoReleaser's nFPM pipe initially: it consumes selected GoReleaser build IDs rather than compiling again and can emit DEB, RPM, and APK packages for each Linux architecture from one package definition. This also creates a simplification opportunity for the OCI path: use nFPM for the intermediate APK and retain melange only if its repository-index or embedded-SBOM behavior proves necessary. nFPM itself is a cross-platform, zero-runtime-dependency packager and does not generate package-repository metadata.

Separate package creation from repository publication. Candidate outputs include the `.deb` and `.rpm` files, checksums, SBOMs, signatures where applicable, and GitHub attestations; they can also be GitHub Release assets. After `release.published`, publisher adapters update the external APT and RPM repositories using those exact package bytes. APT's primary trust boundary is signed `InRelease`/`Release` repository metadata rather than an individual `.deb`; RPM normally uses an embedded package signature as well as signed repository metadata. The RPM package signature therefore belongs in candidate construction, while both repositories' mutable indexes are promotion-stage outputs.

This does not cover Debian, Fedora, or other official distribution repositories. Those ecosystems normally rebuild source under distribution control and should be treated as separate source-package/upstreaming tracks, like MacPorts and Nix, rather than consumers of Meigma's canonical binary packages.

Primary references: https://goreleaser.com/customization/nfpm/ and https://nfpm.goreleaser.com/docs/configuration/.

## 2026-08-17 10:13 — MVP CLI bootstrap
The implementation will proceed as an integrated MVP on a dedicated `mvp` branch, with the user directing scope and sequencing. The first checkpoint is a deliberately small Cobra application shaped like `meigma/template-go`: module `github.com/meigma/release`, executable `cmd/release-mvp`, and command construction under `internal/cli`.

`release-mvp greet [name]` provides a real command path and an `--uppercase` flag. Linker-set `main.version` and `main.commit` values drive `--version`; development fallbacks are `dev` and `none`. Focused Testify tests cover default, named, uppercase, and version output. A smoke build injected `1.2.3` and `abc1234` and produced `release-mvp 1.2.3 (abc1234)` plus the expected greeting. `go test ./internal/cli` passed.

The implementation was committed as `0cc6bf9` (`feat(cli): add release MVP sample`) and pushed to `origin/mvp`.

## 2026-08-17 10:29 — mise toolchain
Rebuilt the template's mise setup rather than copying it. `mise.toml` now pins Go 1.26.6, Python 3.14.7, golangci-lint 2.12.2, uv 0.12.5, Moon 2.5.1, melange 0.59.1, apko 1.2.37, and Cosign 3.1.3. The newer versions were resolved against current upstream releases; `go.mod` moved to Go 1.26.6 to keep the language contract aligned.

The project enables `lockfile` and `locked`, sets `GOTOOLCHAIN=local`, and commits a fresh `mise.lock` with 32 entries covering linux-x64, linux-arm64, macos-x64, and macos-arm64. `mise install` succeeded in locked mode. `mise exec -- go version` selected Go 1.26.6, the environment reported `GOTOOLCHAIN=local`, and `mise exec -- go test ./...` passed.

The template's `image-local` task was intentionally not copied: its melange/apko inputs do not exist yet, so carrying it now would establish a broken task. Add the task with the image configuration when that slice is implemented. Commit `9d23068` (`build(mise): add pinned toolchain`) was pushed to `origin/mvp`.

## 2026-08-17 10:43 — mise PATH resolution
The historic shadowing problem reproduces in the `mvp` worktree. The agent process inherits `MISE_SHELL=zsh`, so `mise doctor` reports `activated: yes`, but the actual non-interactive PATH contains neither mise's shims nor its computed install directories. `shims_on_path` is `no`. Bare `golangci-lint` therefore resolves `~/.local/bin/golangci-lint` 2.11.4 instead of the project pin 2.12.2; bare `moon` resolves `~/.proto/bin/moon` 2.3.5 instead of 2.5.1.

The local `.zshrc` correctly runs `mise activate zsh`, but activation updates direct tool paths through prompt and directory-change hooks. Agent subprocesses start non-interactively and receive their working directory after shell initialization, so that hook does not repair PATH. `mise exec --` is deterministic: it prepends every project install directory, resolved golangci-lint 2.12.2 and Moon 2.5.1, and exported `GOTOOLCHAIN=local`. Prepending `~/.local/share/mise/shims` also resolved 2.12.2 inside the project, but fell through to 2.11.4 outside it; shims resolve from the current directory and do not provide the full mise environment contract.

CI has two distinct behaviors. The template pins `jdx/mise-action` v4.2.0, which adds the mise binary and shim directory through `GITHUB_PATH`; this is normally enough for later steps but remains working-directory- and PATH-order-dependent. Current v4.2.5 (`3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518`) additionally exports the direct PATH computed by `mise env`, preserving order through `GITHUB_PATH`, while still adding shims and automatically using `mise install --locked` when a lockfile exists.

Decision direction: PATH is a convenience, not the automation contract. Agents, scripts, and workflows should enter through `mise run <task>` or `mise exec -- <command>`. CI should pin mise-action v4.2.5 or newer with `export_path: true`, then still invoke the top-level task runner through `mise exec --`; this remains correct if a later action prepends another tool. Local non-interactive convenience may add `mise activate zsh --shims` to `.zprofile`, but it cannot replace `mise exec` because shim resolution depends on the current directory and omits `[env]` behavior. When Moon is added, verify a child task reports the pinned Go and golangci-lint versions rather than assuming `toolchains.default: system` preserves PATH.

## 2026-08-17 10:57 — Moon, CI, and standard linting
Added a minimal Moon 2 workspace with one explicit root project and `format`, `lint`, `build`, `test`, and aggregate `check` tasks. Every task command enters through `mise exec --`; this is intentional even though CI also invokes Moon through `mise exec`. `moon task root:lint` confirmed that Moon still advertises Proto lookup paths ahead of the system path, while the configured task command explicitly re-enters mise and therefore resolves the project pin.

Imported the template's standard `.golangci.yml` with only the module prefix and one template-specific comment adapted. The formatter regrouped the test imports, and the linker metadata globals received the narrow `gochecknoglobals` suppression required by build-time injection. No linter was disabled.

Added the existing CI shape, reduced to the current Go project: checkout, mise setup, Go module/build caches, golangci-lint cache, and Moon CI. It pins `jdx/mise-action` v4.2.5 and mise 2026.8.8. Both `add_shims_to_path` and `export_path` are disabled so the workflow cannot accidentally rely on ambient managed tools; its execution boundary is `mise exec -- moon ci --summary minimal`.

`mise exec -- moon run root:check` passed all format, lint, build, and test tasks. The exact CI command also passed: seven tasks completed, with one cached. Commit `cbee1a5` (`build: add Moon CI and linting`) was pushed to `origin/mvp`.

## 2026-08-17 12:19 — Reusable Go CI prototype
Extracted the CI implementation to `.github/workflows/go-ci.yml` with a `workflow_call` trigger. The reusable contract deliberately has no inputs or secrets: a caller provides the standard `mise.toml`, `mise.lock`, `moon.yml`, `.golangci.yml`, and Go module, and the workflow checks out and tests the caller repository. The repository-facing `.github/workflows/ci.yml` now contains only event filters, concurrency, read-only permissions, and a same-commit local call to `./.github/workflows/go-ci.yml`.

For external consumers, the corresponding call will be `meigma/release/.github/workflows/go-ci.yml@<full-commit-sha>`; no branch reference should be documented as the default. GitHub's reusable-workflow execution retains the caller repository context, so the checkout in the shared workflow retrieves the caller rather than `meigma/release`.

The prototype was tested on GitHub, not only parsed locally. A temporary `mvp` push trigger ran caller workflow `CI` against reusable job `ci / ci`; Actions run 32059599766 completed successfully in 55 seconds, including locked mise setup, caches, and `mise exec -- moon ci --summary minimal`. The temporary branch trigger was then removed. `actionlint` passed on the final caller and reusable workflow files.

Commits `d16efa4` (`test(ci): exercise reusable workflow`) and `f751527` (`chore(ci): remove mvp test trigger`) were pushed to `origin/mvp`.

## 2026-08-17 12:40 — Independent CI review
A read-only DevOps review found the MVP sound: exact action SHAs and matching version comments, least-privilege permissions, checkout credentials removed, locked and provenance-verified mise installation, explicit `mise exec` boundaries, valid Moon configuration, correct full-history checkout, effective format/lint/build/test gates, and a successful real reusable-workflow run.

Two concrete defects deserve early fixes. First, `moon.yml` and `.github/workflows/**` are not task inputs; a PR changing only the gate can make `moon ci` report no affected tasks and exit zero. Add the gate-defining files to the affected input surface. Second, Moon 2.5.1 lacks a `macos-x64` entry in `mise.lock` despite the requested four-platform lock and an available upstream Intel macOS artifact.

Useful hardening candidates: set a job timeout, consider `ubuntu-24.04` instead of `ubuntu-latest`, avoid canceling in-progress `main` push runs, add `filter: blob:none` to the required full-history checkout, and separate CI-only mise tools from the larger release toolchain to reduce the approximately 290 MB cold cache. The rolling `github.run_id` build/lint cache keys trade freshness for continuous cache churn and should be revisited once run data exists. Dependabot and CODEOWNERS belong in later repository-hardening work.

Do not add speculative reusable-workflow inputs yet. Optional inputs can be added backward-compatibly when a real consumer needs variation; the current no-input contract is intentional. The first external consumer must still test caller-repository checkout semantics, repository Actions access policy, and its required standard files.

## 2026-08-17 12:57 — CI review fixes

Closed the affected-file no-op by adding a `ciConfig` file group for `moon.yml` and `.github/workflows/**/*.yml` and including it in every executable task. Hardened the reusable job with a 20-minute timeout, `ubuntu-24.04`, and blobless full-history checkout. The caller now cancels overlapping pull-request runs but preserves every `main` push run.

The Aqua registry excludes Intel macOS for Moon 2.5.1 even though upstream publishes the binary. Moon now uses mise's HTTP backend with upstream per-archive SHA-256 files; the generated lock contains checksum-pinned Linux amd64/arm64 and macOS amd64/arm64 entries. `mise install --locked`, `mise exec -- moon --version`, `actionlint`, and local `mise exec -- moon ci --summary minimal` passed.

A temporary `mvp` trigger exercised the hardened reusable workflow on GitHub. Actions run 32063069655 completed successfully in 21 seconds, including the new locked Moon backend and all Moon CI tasks. Commits `3389107` (`fix(ci): harden reusable workflow`) and `e354b31` (`chore(ci): remove review test trigger`) were pushed to `origin/mvp`.

## 2026-08-17 13:09 — Release Please configuration

Adapted the template's manifest-mode Release Please configuration for the MVP. The package is `release-mvp`, uses the Go strategy, unscoped `v*` tags, draft GitHub releases, forced tag creation, pre-1.0 bump rules, and the existing changelog sections. The manifest starts at the never-released sentinel `0.0.0`, while `initial-version: 0.1.0` makes the first release explicit. Template `extra-files` were omitted because `melange.yaml` and `apko.yaml` do not exist in the MVP yet.

Both files passed the Release Please 17.11.1 JSON schemas. A live API-backed `manifest-pr --dry-run` against `origin/mvp` proposed `chore(mvp): release 0.1.0` and updates only `CHANGELOG.md` plus `.release-please-manifest.json`. The first dry run exposed the Go strategy's default first release of `1.0.0`; adding `initial-version` corrected it.

The Release Please workflow was not copied yet. Neither the repository nor the Meigma organization currently exposes the template's `MEIGMA_RELEASE_APP_ID` variable or `MEIGMA_RELEASE_APP_PRIVATE_KEY` secret, so enabling that workflow now would fail on the first `main` push. Commits `159140a` (`build(release): add release-please config`) and `1afe4a7` (`fix(release): start releases at v0.1.0`) were pushed to `origin/mvp`.

## 2026-08-17 14:36 — Meigma Release GitHub App

Created the organization-owned private GitHub App `meigma-release` (App ID `4628617`). It has only repository metadata read plus contents, issues, and pull requests read/write; webhooks, OAuth installation authorization, organization permissions, and Actions write access are disabled.

Installation `154493041` is restricted to `meigma/release`. The organization variable `MEIGMA_RELEASE_APP_ID` and Actions secret `MEIGMA_RELEASE_APP_PRIVATE_KEY` both use selected-repository visibility and are also restricted to `meigma/release`. The generated private-key download was uploaded directly to the organization secret and removed from local temporary storage.

GitHub API checks confirmed the app owner, ID, permission set, selected-repository installation mode, credential visibility, and both credential repository scopes. The Release Please workflow can now mint a short-lived installation token through these centralized credentials.

## 2026-08-17 15:02 — Authenticated Release Please workflow

Added `.github/workflows/release-please.yml` on `mvp`. The final workflow runs on `main` pushes or manual dispatch, mints a repository-scoped installation token with `actions/create-github-app-token` v3.2.0, and runs `googleapis/release-please-action` v5.0.0 against the manifest configuration. Both actions are pinned by full commit SHA; the job uses Ubuntu 24.04 with a 10-minute timeout.

The first live rehearsal exposed that `create-github-app-token` now deprecates `app-id` in favor of `client-id`. The organization credential is therefore `MEIGMA_RELEASE_APP_CLIENT_ID`, selected-repository scoped to `meigma/release`; the obsolete `MEIGMA_RELEASE_APP_ID` variable was deleted. This corrects the variable name recorded in the preceding GitHub App checkpoint. The private-key secret name and scope are unchanged.

Three temporary `mvp` runs exercised token creation and Release Please. Runs `32073789780`, `32073890921`, and `32073942559` succeeded; the client-ID runs emitted no deprecation annotation. The app authored pull request #1, and updating that PR triggered CI run `32073969078`, which passed in 24 seconds. This proves the App token preserves downstream workflow fan-out. The rehearsal PR was closed and its generated branch deleted.

Temporary `mvp` triggers and CI filters were removed. The final remote workflow targets only `main`; `actionlint` and local `mise exec -- moon ci --summary minimal` both passed. Repository rulesets are currently empty, so no protected-tag bypass is needed yet. Final workflow commits are `12ff06f` (`ci(release): add release-please workflow`) and `aeefd90` (`fix(release): use GitHub App client ID`).

## 2026-08-17 15:08 — Initial GoReleaser configuration

Added `.goreleaser.yaml` for review. It builds the pure-Go `release-mvp` command with `CGO_ENABLED=0` for Darwin, Linux, and Windows on amd64 and arm64. Darwin and Linux ship as `tar.gz`; Windows ships as `zip`. Archive names use `<project>_<version>_<os>_<arch>`, and linker flags inject the release version plus full commit while removing local paths and build IDs.

The draft enables tagged-build module proxying, deterministic commit timestamps, SHA-256 checksums, an SBOM per archive, and a Cosign v3 bundle signature over `checksums.txt`. Release Please remains the release-note and draft owner: GoReleaser disables its changelog, keeps existing notes, and uploads into the existing draft without replacing it. Channel-specific Homebrew, MacPorts, Nix, Scoop, mise, OCI, and installer publication is intentionally absent from this first config.

GoReleaser 2.15.4 and current 2.17.1 both accepted the configuration. A 2.17.1 snapshot build with signing skipped produced six platform archives, six archive SBOMs, and a checksum manifest in eight seconds. The native Darwin arm64 binary reported `0.0.0-SNAPSHOT-aeefd90` and the full source commit. Archive inspection confirmed `release-mvp` on Darwin and `release-mvp.exe` on Windows. Cosign keyless signing was not exercised locally because it requires the eventual CI OIDC identity. Commit `5ac6b9f` (`build(release): add GoReleaser config`) was pushed to `origin/mvp`.

## 2026-08-17 15:47 — Authoritative release-assets workflow

Added the first pre-publish stage. `.github/workflows/go-pre-publish.yml` is a reusable workflow that requires a tag ref, checks out full history without persisted credentials, installs pinned Go, Syft 1.51.0, Cosign 3.1.3, and GoReleaser 2.17.1 tooling, then runs `goreleaser release --clean --skip=publish`. It validates every file named by `checksums.txt`, requires the Cosign bundle, and uploads only the six archives, six archive SBOMs, checksum manifest, and checksum signature as one immutable `release-assets` Actions artifact. The reusable workflow exposes the artifact ID, URL, and SHA-256 digest. It has only `contents: read` and `id-token: write`; it does not create or modify a GitHub Release.

`.github/workflows/pre-publish.yml` is the thin `v*` tag caller, matching the repository's existing reusable-workflow plus trigger-wrapper convention. It refuses tag-deletion work, keeps cancellation disabled for releases, and grants the reusable workflow its required permissions. The artifact is retained for seven days and uploaded without redundant compression.

Three tag rehearsals were useful. Run `32076834493` exposed the Node 20 deprecation in `actions/upload-artifact` v4.6.2, so the workflow moved to current v7.0.1 on Node 24. Run `32077024659` proved the corrected asset build. Final wrapper run `32077359878` succeeded without annotations in 1 minute 46 seconds at commit `ab509b6`. Its artifact `9303938755` was 9,800,684 bytes with digest `sha256:ddace359572f79bf9e5491c79cab890a5e8c9f9c589090f6347d764de1ff0646`.

The final artifact was downloaded and checked independently: all twelve checksummed archives and SBOMs passed SHA-256 verification; Cosign verified `checksums.txt` against issuer `https://token.actions.githubusercontent.com` and exact identity `https://github.com/meigma/release/.github/workflows/go-pre-publish.yml@refs/tags/v0.0.0-e2e.3`; and the Darwin arm64 binary reported version `0.0.0-e2e.3` plus source commit `ab509b6cb972b578f3a60115b2732561331b3306`. All temporary rehearsal tags were removed locally and remotely. Final `actionlint` and `mise exec -- moon ci --summary minimal` checks passed. Workflow commits are `ecec3f9`, `d39ea53`, and `ab509b6`.

## 2026-08-17 16:29 — mise-owned release toolchain

Replaced `actions/setup-go` in `.github/workflows/go-pre-publish.yml` with the repository's pinned `jdx/mise-action` v4.2.5 action. GoReleaser 2.17.1 is now declared and locked beside Go 1.26.6 in `mise.toml` and `mise.lock`; the action installs only `go` and `aqua:goreleaser/goreleaser`. The release command runs inside `mise exec`, asserts that the active `go` executable is the mise-managed executable, and keeps `GOTOOLCHAIN=local` so Go cannot silently download another toolchain. `MISE_EXEC_AUTO_INSTALL=false` prevents `mise exec` from installing unrelated project tools.

Three temporary tag runs refined the change. Run `32080058323` exposed that a mise-managed command cannot see GoReleaser until it is declared as a project tool. Run `32080193114` succeeded after adding GoReleaser but showed that the default action installation eagerly installed every tool in `mise.toml`. Final run `32080433803` installed only locked Go 1.26.6 and GoReleaser 2.17.1; the action reported every unrelated tool as missing, and the release process used `/home/runner/.local/share/mise/installs/go/1.26.6/bin/go`.

Final run `32080433803` succeeded in 1 minute 37 seconds at commit `787fc76`. Artifact `9304953622` was 9,800,620 bytes with digest `sha256:16da4064d95c357ad8d9ed9964316ae1bca947d78939da3a9842554aff8ca086`. Independent verification passed for all twelve checksummed archives and SBOMs; Cosign accepted the exact reusable-workflow identity for tag `v0.0.0-e2e.6`; and the Darwin arm64 binary reported `0.0.0-e2e.6` plus source commit `787fc76a3e0fdd1c41a1096166e0d15dcb040996`. All temporary mise rehearsal tags were removed locally and remotely. Local `actionlint`, GoReleaser configuration validation under the locked mise environment, and `mise exec -- moon ci --summary minimal` passed. Workflow commits are `0972d26`, `9e15876`, and `787fc76`.

## 2026-08-17 16:38 — Unified mise release toolchain

Moved Syft 1.51.0 and Cosign 3.1.3 under the same mise ownership as Go and GoReleaser. `mise.toml` and `mise.lock` now define all four release executables, and the reusable workflow installs only those four locked tools. The dedicated `anchore/sbom-action/download-syft` and `sigstore/cosign-installer` steps were removed. Before invoking GoReleaser, the workflow now proves that `go`, `goreleaser`, `syft`, and `cosign` each resolve to the corresponding `mise which` path.

Tag rehearsal run `32081114121` succeeded in 1 minute 17 seconds at commit `07a4c63`. mise checksum-verified Syft, Cosign-verified Cosign, verified GoReleaser's GitHub artifact attestations, and installed the locked Go toolchain. Logs confirmed all four executables came from `/home/runner/.local/share/mise/installs`; unrelated project tools remained missing. GoReleaser produced all six archives, all six SBOMs, and the Cosign bundle.

Artifact `9305159060` was 9,800,380 bytes with digest `sha256:8f47430e5489c2e510a24dd473cf50b2a498cabb7f8e91b76411d399abf28341`. Independent verification passed for every checksummed archive and SBOM; Cosign accepted the exact reusable-workflow identity for tag `v0.0.0-e2e.7`; and the Darwin arm64 binary reported `0.0.0-e2e.7` plus source commit `07a4c63ec2b0569378111584080f7875ab84d62f`. The rehearsal tag was removed locally and remotely. Local `actionlint`, all four path assertions and version checks, GoReleaser configuration validation, and `mise exec -- moon ci --summary minimal` passed. Workflow commit: `07a4c63` (`refactor(release): unify mise toolchain`).

## 2026-08-17 17:05 — GitHub Release publication

Added `.github/workflows/go-publish-github-release.yml`, a reusable publisher that consumes only the authoritative Actions artifact ID and digest. It verifies the artifact metadata through the GitHub API, locates the empty draft created for the current tag, downloads with `digest-mismatch: error`, rechecks every checksum and the exact pre-publish Cosign identity, creates GitHub-native build-provenance attestations from `checksums.txt`, uploads all fourteen assets without clobbering, and verifies the uploaded names, sizes, and GitHub-computed digests before optionally publishing. The Release App token is confined to release mutation; the workflow's `GITHUB_TOKEN` retains only the read and attestation permissions required by GitHub-native actions.

The tag wrapper is now `.github/workflows/release.yml`. It builds the authoritative artifact once, passes the artifact ID and digest to the publisher, supplies `MEIGMA_RELEASE_APP_CLIENT_ID` and `MEIGMA_RELEASE_APP_PRIVATE_KEY`, and enables final publication. GitHub CLI 2.97.0 is pinned in `mise.toml` and `mise.lock`; the publisher installs only locked `gh` and Cosign through `jdx/mise-action`.

The first two publication runs exposed API contract details. Run `32082081958` showed that `actions/upload-artifact` emits its digest without the `sha256:` prefix while the artifacts API includes it, so comparison now normalizes either representation. Run `32082272846` showed that the by-tag releases endpoint does not discover an unpublished draft; the workflow now paginates the releases collection and selects the exact tag with `jq`.

Draft-only run `32082671675` then succeeded at commit `bec93e2`. Artifact `9305632676` was 9,800,625 bytes with digest `sha256:126eefaada2fa6e5a276f12b61967e21748f48963484186ef69e1969a1dcd394`. The release remained a draft with fourteen assets. All twelve archives and SBOMs passed independent checksum verification, Cosign accepted the exact pre-publish workflow identity, GitHub attestation verification accepted the publisher workflow identity, and the downloaded Darwin arm64 binary reported `0.0.0-e2e.10` plus commit `bec93e2e2bdc61fa3898c0042a7d60a5bf713838`.

Public run `32082936409` succeeded at commit `8911ad1`. Artifact `9305711289` was 9,800,632 bytes with digest `sha256:8b3cd08fdd7ff5e47d6aaaad6afb49ad6663c7025b029996a622b9762d969f5f`. The draft became a public, non-prerelease release with fourteen assets. Independent verification passed for all twelve checksummed archives and SBOMs, the Cosign bundle, and all twelve GitHub build-provenance attestations constrained to `meigma/release/.github/workflows/go-publish-github-release.yml` and `refs/tags/v0.0.0-e2e.11`. The downloaded Darwin arm64 binary reported `0.0.0-e2e.11` plus commit `8911ad105e3a11860d9eabffa81e3e597784e6ec`.

All disposable releases, tags, and downloaded files were removed. No `v0.0.0-e2e.*` tag or release remains. The final MVP worktree is clean and synchronized with `origin/mvp`; local `actionlint` and `mise exec -- moon ci --summary minimal` passed. Publication commits are `84af48a`, `3f3d17d`, `bec93e2`, and `8911ad1`.

## 2026-08-17 17:24 — Language-neutral GitHub publisher

Renamed `go-publish-github-release.yml` to `publish-github-release.yml` and removed its remaining Go assumptions. The publisher now accepts `checksum-signing-workflow` as part of its reusable-workflow contract and constructs the exact Cosign certificate identity from the caller repository, supplied producer workflow path, and current tag. The Go-specific `release.yml` caller passes `.github/workflows/go-pre-publish.yml`; the publisher itself no longer names a Go workflow.

The signed checksum manifest is now the authoritative payload list. Bundle validation accepts only flat, safe filenames; rejects empty manifests, duplicate entries, control-file entries, missing files, symlinks, directories, and files absent from `checksums.txt`; verifies every digest and the signed manifest; and writes the exact upload list. Publication uploads only those listed payloads plus `checksums.txt` and `checksums.txt.sigstore.json`. Uploaded-release inspection now paginates beyond the first 100 assets.

Public rehearsal run `32084155065` succeeded at commit `799e830`. Artifact `9306132662` was 9,800,579 bytes with digest `sha256:d928061cdc3462da6727ba24f44d587227d931039f7ab00f9e96ebe4092147a7`. The generalized publisher accepted the Go producer's bundle, uploaded fourteen assets, verified their GitHub-computed digests, created attestations, and published the draft.

Independent verification passed for all twelve checksummed archives and SBOMs, the Cosign signature constrained to `.github/workflows/go-pre-publish.yml`, and all twelve GitHub provenance attestations constrained to `.github/workflows/publish-github-release.yml` and `refs/tags/v0.0.0-e2e.12`. The downloaded Darwin arm64 binary reported `0.0.0-e2e.12` plus commit `799e83029b38e1f7efe87c6eb1114e0647eba5af`. The disposable release, tag, and downloaded files were removed. Local `actionlint` and `mise exec -- moon ci --summary minimal` passed. Implementation commit: `799e830` (`refactor(release): generalize GitHub publisher`).

## 2026-08-17 17:43 — JavaScript publication control plane

Replaced every Bash block in `.github/workflows/publish-github-release.yml` with focused inline JavaScript steps using `actions/github-script` 9.0.0, pinned to commit `3a2844b7e9c422d3c10d287c895573f7108da1b3` and its Node 24 runtime. GitHub API operations now use Octokit objects rather than `gh api` plus `jq`: artifact verification uses the job token, while draft discovery, paginated asset inspection, and final publication use a second client created from the narrowly scoped Release App token. Network-facing steps request three retries.

Bundle parsing, filename validation, duplicate detection, and streaming SHA-256 calculation now use Node standard-library APIs. Cosign verification and `gh release upload` remain the appropriate external tools, but `actions/exec` invokes them through argument arrays under the locked mise environment; no shell parses their arguments. GitHub expression values enter scripts through environment variables rather than direct script interpolation. The publisher contains no `shell: bash`, shell `run` block, `jq`, `sha256sum`, word splitting, or Bash array state.

Public rehearsal run `32085382502` succeeded at commit `9b8efce`. Artifact `9306542522` was 9,800,575 bytes with digest `sha256:0a66efc127b40981c99a2d87b6ea5900d75065941a9c659910e91ed2dfed2db3`. All seven JavaScript steps completed: tag validation, tool verification, artifact handoff, draft discovery, bundle verification, upload, and final release verification.

The release became public with fourteen assets. Independent verification passed for all twelve checksummed archives and SBOMs, the exact Go producer Cosign identity, and all twelve GitHub provenance attestations constrained to the generic publisher and `refs/tags/v0.0.0-e2e.13`. The downloaded Darwin arm64 binary reported `0.0.0-e2e.13` plus commit `9b8efcea336838af3ec263052d2da2ea635b4a43`. Local checks compiled all seven inline scripts as async functions, passed `actionlint`, and passed all seven Moon tasks. The disposable release, tag, and downloaded files were removed. Implementation commit: `9b8efce` (`refactor(release): replace publisher Bash with JavaScript`).

## 2026-08-17 18:23 — Resumable publication and signing boundary

Applied the independent DevOps review findings. The publisher no longer rejects a populated draft. After bundle verification it requires the tag to resolve uniquely to the previously validated release ID, rejects only asset names outside the signed manifest, and uploads the complete expected set with `--clobber`; the existing digest comparison still proves the final contents. Artifact handoff now rejects expired artifacts and artifacts from another workflow run. Post-upload verification polls until every expected asset reports `state: uploaded` and a digest before comparing the closed name and digest sets.

Removed `go test ./...` from GoReleaser's `before.hooks`, so repository and test-dependency code no longer executes in the OIDC-enabled signing job. CI remains the test gate. The inert GoReleaser draft/publication settings were replaced by `release.disable: true`, while `--skip=publish` remains as defense in depth. `goreleaser check` accepted the disabled release pipe.

The recovery rehearsal used the same draft twice. Draft-only run `32087719073` succeeded at commit `e9fd9f8`, leaving release ID `372034549` unpublished with fourteen assets. Its artifact `9307287353` was 9,800,699 bytes with digest `sha256:c4822cc1089b1ada6b765ea1274865a365e88007695ef2b9d2f7d595770d0000`. The tag was then advanced to the final-publication commit without deleting the populated draft. Resume run `32087869532` succeeded at commit `cd9553a`, converged the same fourteen-asset release with `--clobber`, and published the same release ID. Its artifact `9307329423` was 9,800,664 bytes with digest `sha256:763ae375daaf97f022d993470320c5c7ca653f383bcbca43acc821559265a040`.

Independent verification passed for all twelve final archives and SBOMs, the exact Go producer Cosign identity, and all twelve GitHub provenance attestations. The downloaded Darwin arm64 binary reported `0.0.0-e2e.14` plus final commit `cd9553a7a76820950bd4ded3cfb38b2022045fbb`. Final `actionlint`, `goreleaser check`, and all seven Moon tasks passed. The disposable release, moved tag, and downloaded files were removed. Implementation commits are `e9fd9f8` (`fix(release): make publication resumable`) and `cd9553a` (`fix(release): restore final publication`).

## 2026-08-17 19:16 — Cross-repository consumer rehearsal

Changed the publisher contract from a caller-repository-relative signing path to the exact `checksum-signing-workflow-ref` expected in the Cosign certificate identity. The same-repository caller still derives its tagged workflow ref from GitHub context; external consumers can now name an immutable producer workflow revision. Implementation commit: `5be87cc` (`feat(release): accept external signer workflow refs`).

Created disposable public repository `meigma/release-consumer-e2e` with one minimal Go command, GoReleaser and locked mise configuration, Release Please, and a forty-line tag caller. Its two jobs called `meigma/release/.github/workflows/go-pre-publish.yml` and `publish-github-release.yml`, both pinned to full commit `5be87cc60f2f11ac11fe401d8129c7644edc17ca`; no build or publication implementation was copied into the consumer. The organization variable, secret, and selected-repository Release App installation supplied its release credentials.

Initial Release Please run `32090689754` created PR `#1` (`chore(main): release 0.1.0`). Merging it produced commit `c8be6dce118107a572fee3d8bcd25a1ab63f1349`; Release Please run `32090731833` then created tag `v0.1.0` and its matching draft. The App-created tag triggered downstream Release run `32090742756`, proving the complete versioning-to-publication event chain. That draft-only run left release ID `372048183` unpublished with fourteen assets. Artifact `9308241857` was 4,365,037 bytes with digest `sha256:9d6209be68d381b6131ae6b58a7ccacd0b03f306cdc893cc880beb598bdc47e9`.

For the disposable recovery test, the caller enabled final publication and the tag was advanced to commit `eb60c020aac5a66f2eeffb39d579059ff5ef6196` without deleting the populated draft. Resume run `32090913380` successfully rebuilt, re-signed, attested, uploaded with `--clobber`, and published the same release ID with the same fourteen asset names. Artifact `9308288928` was 4,365,126 bytes with digest `sha256:7cb444b1152276aaf29013355115bc824ed156f440a3398d4032027d7b86b6f9`.

Independent verification passed all twelve archive and SBOM checksums. The checksum bundle matched exact external producer identity `https://github.com/meigma/release/.github/workflows/go-pre-publish.yml@5be87cc60f2f11ac11fe401d8129c7644edc17ca`. All twelve GitHub attestations verified against external publisher `meigma/release/.github/workflows/publish-github-release.yml`, consumer repository `meigma/release-consumer-e2e`, and source ref `refs/tags/v0.1.0`. The downloaded Darwin arm64 binary reported `release-consumer 0.1.0 (eb60c020aac5a66f2eeffb39d579059ff5ef6196)`.

Final `actionlint` and all seven Moon tasks passed in `meigma/release`. The disposable repository, release, tags, Actions history, selected App access, organization secret and variable grants, and local rehearsal files were removed. The organization repository count returned from fifty to forty-nine.

## 2026-08-17 19:51 — GitHub Release consumer documentation

Productized the proven cross-repository boundary without adopting it in a real consumer. Added a public repository overview, a complete GitHub Release contract reference, and separate how-to guides for configuration, draft rehearsal and recovery, and immutable workflow upgrades. The upgrade procedure requires full-SHA review, atomic migration of the whole caller contract, target-aware signing and asset checks, a draft rehearsal, and complete rollback of repository and external prerequisite changes before publication.

Added `examples/go-release/`, a copyable release-only consumer with a dependency-free Go command, GoReleaser configuration, Release Please configuration, two thin calling workflows, and a five-tool mise lock covering Linux and macOS on amd64 and arm64. The example pins both reusable workflows and the checksum certificate identity to the rehearsed revision `5be87cc60f2f11ac11fe401d8129c7644edc17ca` and defaults to `publish-release: false`. It deliberately excludes consumer CI policy and real-repository adoption.

Two technical-writer agents drafted the reference and task-oriented guides under the `language-style` and Diátaxis rules. A conformance review found stale-run recovery, ambiguous run selection, unsupported proof phrasing, limited draft pagination, incomplete upgrade migration/rollback, API diff truncation, draft download, and target-contract override defects. All findings were corrected; the final conformance verdict passed.

Validation passed for all internal Markdown links, all 36 documented Bash blocks, YAML and JSON parsing, immutable action and reusable-workflow refs, `actionlint`, locked mise installation and lock regeneration, `goreleaser check`, `go test ./...`, injected `--version` behavior (`example 0.1.0 (deadbeef)`), and all seven Moon tasks. Implementation commit: `9c13e9b` (`docs(release): document consumer adoption`).

## 2026-08-17 22:03 — nFPM APK byte-preservation prototype

Added one GoReleaser nFPM definition that consumes the existing `release-mvp` build ID and emits APK packages for the Linux targets without another compilation step. Snapshot `0.0.0-SNAPSHOT-9c13e9b` produced `release-mvp_0.0.0_SNAPSHOT-9c13e9b_x86_64.apk` and `release-mvp_0.0.0_SNAPSHOT-9c13e9b_aarch64.apk`. Their `.PKGINFO` records identify the expected package name, version, architecture, description, homepage, and public Meigma maintainer address.

The nFPM boundary preserves the authoritative executable exactly. The amd64 canonical binary and `/usr/bin/release-mvp` extracted from the x86_64 APK both have SHA-256 `fd7b996a31aa6a1d124b5d005eea42f28d40f545e1e5101867244418fcf7b2ae`. The arm64 pair both have SHA-256 `fa349034dbaa4f14470422fd70629249935ab373c2f881d09ce9d67310bf61be`. `cmp` also passed for both pairs. APK archive headers install the static ELF executables as root-owned mode `0755`; nFPM did not strip or otherwise mutate them.

The packages are currently unsigned, and `.PKGINFO` has no license field because this repository has no license declaration. Neither blocks the byte-preservation result. Repository indexing, repository signing, and the project license remain inputs to the apko composition experiment.

`goreleaser check`, the complete snapshot build, `git diff --check`, and all seven Moon tasks passed. Implementation commit: `4ab8f9d` (`feat(release): package canonical binaries as APKs`). Decision: nFPM passes the first OCI adapter gate; test a minimal indexed APK repository before introducing Melange.

## 2026-08-17 22:18 — APK-to-apko OCI composition prototype

Turned the nFPM APKs into architecture-specific signed repository indexes with `melange index`, then generated an apko lock covering both application packages and the exact Alpine 3.24 runtime packages, repository indexes, and signing keys. The locked apko configuration contains only `alpine-release`, `ca-certificates-bundle`, and `release-mvp`; sets `/usr/bin/release-mvp` as the entrypoint; runs as UID/GID 65532; and carries source, version, revision, title, and description OCI annotations. It produces a two-platform OCI index for `linux/amd64` and `linux/arm64` without a Dockerfile or another Go compilation.

The nFPM-backed image preserved both canonical executables byte-for-byte through GoReleaser, APK extraction, and final apko layer extraction. The amd64 digest remained `fd7b996a31aa6a1d124b5d005eea42f28d40f545e1e5101867244418fcf7b2ae`; arm64 remained `fa349034dbaa4f14470422fd70629249935ab373c2f881d09ce9d67310bf61be`. Docker ran both final architecture images as UID 65532 and each reported `release-mvp 0.0.0-SNAPSHOT-9c13e9b (9c13e9bc29e9f15b15ef2504dc437fa80f072859)`. The CA bundle and nonroot account files were present. Repeating the locked build with the same build timestamp produced byte-identical OCI layouts and SPDX files; the OCI index digest was `sha256:d4d14ff9e05f85fc75c321afcb1249d5276f15cca5f4c38568efff2884f6cf2d`.

Compared that minimal path with Melange `v0.59.1` acting only as an adapter: one `install` command copied each prebuilt GoReleaser binary into an APK without recompiling or stripping it. Both extracted package binaries and both final apko image binaries retained the same canonical digests. Melange additionally produced signed APKs, signed repository indexes, embedded SPDX package SBOMs, `cmd:release-mvp` metadata, and SLSA provenance archives. apko composed the embedded SBOM into the final image SBOM, preserving the application package, source configuration, and commit relationship that was absent from the nFPM-backed image SBOM. The Melange APKs were only about 2.2–2.5 KiB larger than the nFPM APKs. Repeated Melange builds with the same key and build date produced identical APKs and indexes; provenance archives differed because their run timestamps are intentionally current.

One local-runner defect remains to verify on Linux: Melange's Docker runner copied packaged payload ownership back as macOS UID 501 even when `install -o 0 -g 0` was requested. apko preserved that UID in the image, although mode `0755` and the nonroot runtime both worked. nFPM produced root-owned payloads. A GitHub-hosted Linux rehearsal must require UID/GID 0 for the installed executable before production adoption.

Decision: use Melange, not nFPM plus custom indexing and SBOM glue, as the production APK adapter between canonical GoReleaser Linux binaries and apko. nFPM proved that the build-once boundary works, but Melange supplies the package signing, repository indexing, embedded application SBOM, provenance, and APK-native metadata that the OCI lane needs while still preserving the authoritative binary. The intended lane is now GoReleaser canonical binary → Melange architecture-specific APK repository → locked apko multi-architecture OCI layout. The next prototype should implement that lane in an unprivileged reusable producer workflow, retain the OCI layout as the authoritative workflow artifact, and resolve the Linux ownership and APK signing-key lifecycle before adding GHCR publication.

## 2026-08-17 23:15 — Reusable OCI producer and cross-repository rehearsal

Implemented the production producer as `.github/workflows/go-oci-build.yml`. It accepts the canonical Linux binary artifact ID and SHA-256 from `go-pre-publish.yml`, verifies that the artifact belongs to the current workflow run, downloads exactly that artifact, and stages its amd64 and arm64 GoReleaser `Binary` records. Melange copies those binaries without recompiling them into architecture-specific signed APK repositories. apko locks the application and Alpine runtime package set, composes a `linux/amd64` plus `linux/arm64` OCI layout, generates SPDX image SBOMs, and retains the public APK repository key, signed APKs and indexes, Melange provenance, apko lock, configs, canonical-binary checksums, layout, and image digest in the `oci-image` workflow artifact. The caller blocks GitHub Release publication on successful OCI production but does not publish the image to a registry.

The local production-path rehearsal built both APKs, proved their application bytes equal the canonical GoReleaser binaries, verified root-owned mode `0755` package payloads on Linux, composed the locked image, matched both final layer binaries to the canonical SHA-256 values, and ran both Docker platforms as UID 65532. The final amd64 and arm64 images reported the expected version and commit, carried the expected entrypoint, CA bundle, account files, OCI annotations, and package plus image SBOMs, and repeated lock-driven composition was byte-identical.

The disposable public consumer repository `meigma/release-oci-e2e` exposed two hosted-runner assumptions before the successful rehearsal. First, `actions/upload-artifact` and `actions/download-artifact` do not retain executable mode, so the producer must accept a regular canonical input file and explicitly install it as mode `0755`. Second, an amd64 GitHub-hosted runner needs QEMU binfmt registration before Melange can execute the arm64 builder; the workflow now uses the immutable `docker/setup-qemu-action` v3.6.0 commit and a digest-pinned `tonistiigi/binfmt` image for arm64 only.

Cross-repository tag `v0.1.3` completed successfully in run `32105846771`: authoritative GoReleaser assets finished in 55 seconds, and the OCI producer finished in 30 seconds. Every OCI step passed, including artifact identity verification, QEMU setup, signed APK creation, locked apko composition, image verification, and artifact upload. Artifact `9313123992` is named `oci-image`, has GitHub artifact digest `sha256:2c1b030f47aa9c8d775b6728c085443574c7ebef0fdde3bd226a1a65e010aeab`, and contains a two-platform layout whose index SHA-256 matches `image-digest.txt`: `sha256:20056d89183e28c6031d7b555bce4e2bf6cc68c28a3ba3e96c9fea8d1364e2ae`.

Implementation commits: `f880a4c` (`feat(release): build canonical OCI image artifacts`), `c0cdf1d` (`fix(release): restore executable OCI inputs`), `5904570` (`fix(release): emulate ARM OCI package builds`), and `4956253` (`docs(release): pin validated OCI producer`). `actionlint` passed for every root and example workflow, both GoReleaser configurations passed `goreleaser check`, the complete snapshot and canonical handoff checksum checks passed, and all seven Moon tasks passed.

## 2026-08-18 09:20 — Signed GHCR publisher and cross-repository rehearsal

Implemented `.github/workflows/publish-oci-image.yml` as the privileged counterpart to the unprivileged OCI producer. It accepts only the current run's `oci-image` artifact ID, GitHub artifact digest, and expected image index digest; revalidates the complete two-platform layout and SPDX files; logs in to `ghcr.io` with the job token; uploads the existing blobs and manifests without rebuilding; and requires `MAJOR.MINOR.PATCH`, `MAJOR.MINOR`, `MAJOR`, and `latest` to resolve to the exact staged digest. It then signs the image index and both platform manifests recursively with keyless Cosign, creates GitHub and registry-backed provenance for the index, and creates GitHub and registry-backed SPDX SBOM attestations for each platform. The consumer caller now publishes the GitHub Release only after OCI publication succeeds.

The first hosted run exposed two publisher defects. The initial `jdx/mise-action` commit was not resolvable and was replaced with the verified `v4.2.5` commit. More importantly, `oras cp --from-oci-layout layout@<sha256(index.json)>` cannot address an OCI layout's top-level `index.json`: that digest is not one of the descriptors listed inside the layout index. The corrected publisher uploads every referenced config and layer blob, pushes each platform manifest by its immutable digest, then pushes the original `index.json` bytes under all four release tags. This retains the producer's authoritative index digest instead of rebuilding or rewriting the index.

Disposable public consumer `meigma/release-oci-publish-e2e` tag `v0.1.0` completed all three jobs in run `32159151961`. Tags `0.1.0`, `0.1`, `0`, and `latest` all resolve to `sha256:d21aa9052c266eb69fddf92e25aec2f2919ff8969af043082e677ca896c663d2`. The published amd64 and arm64 manifests are `sha256:c25075ec0a0c7c632806b6cabe26be957e62c60734af52b346eee7dc1b915be8` and `sha256:be70c13528c1ea98d37eeb9478cdc77ff88e59380cba0057bd6c52f7f26a7572`. Exact-identity Cosign verification passed for the index and both platform manifests. GitHub API and registry-bundle provenance verification passed for the index; GitHub API and registry-bundle SPDX 2.3 verification passed for both platform manifests. Docker pulled and ran both platforms, and each reported `example 0.1.0 (cea78dba0f31d21f46dfa0aad75569c426802017)`. The GHCR package is public.

Implementation commits: `33fb917` (`feat(release): publish signed OCI images`), `6380796` (`fix(release): pin resolvable mise action`), `7294599` (`fix(release): publish OCI layout manifests`), and `09312cc` (`docs(release): document OCI image publication`). The reusable publisher revision is `72945990eda349f83c0f7628e85521fb30071fc6`. `actionlint`, `goreleaser check`, offline documentation link validation, `git diff --check`, and all seven Moon tasks passed.

## 2026-08-18 12:05 — OCI publication safety remediation

Remediated the OCI review findings in immutable workflow revision `052e8277da00bf6369093ed8736cf5d21195d843`. The publisher now validates the exact `MAJOR.MINOR.PATCH` tag before any upload, rejects a different existing digest, serializes all OCI publications for a consumer repository, and advances `MAJOR.MINOR`, `MAJOR`, and `latest` only when the candidate stable version is newer. It uploads the authoritative layout by digest, signs the index and both platform manifests, creates the index provenance and both platform SBOM attestations, and applies public tags only after all trust metadata succeeds. The GitHub Release publisher now requires the successful digest-pinned OCI output before making the draft public.

The OCI builder now verifies the actual ELF architecture and static linkage of both canonical binaries, the final layer entry mode and ownership, exact entrypoint, numeric runtime user `65532`, and exact source, version, revision, title, description, and license annotations on both manifests and configs. Documentation now verifies each platform Cosign signature, names the actual `apko.lock.json` artifact, describes immutable and monotonic tag behavior, and directs transient publisher recovery through failed-job reruns that preserve the original builder artifact.

Disposable public consumer `meigma/release-oci-remediation-e2e` proved the behavior. Tag `v0.2.0` completed run `32173193142` and published index `sha256:68989107f60bf01fcf06cfd5cf32756dd3a6ae51764806f01e744bcac9528d30`; tags `0.2.0`, `0.2`, `0`, and `latest` resolve to it. The out-of-order `v0.1.1` run `32173532333` published index `sha256:2bdc7794a3f9bc89247815e417066bf0a4f84cf662204bd9059b496ff2dcc0b0`; `0.1.1` and `0.1` resolve to that digest while `0` and `latest` remain on `0.2.0`. After moving `v0.1.1` to different content, run `32173846050` failed at `Plan OCI image tags`; every upload, signature, attestation, and tag step was skipped, and all existing tag digests remained unchanged. Its failed-job-only retry reused the original successful build jobs and failed at the same pre-upload guard.

Exact-identity Cosign verification passed for both release indexes and all four platform manifests. GitHub provenance verification passed for both indexes, registry-backed SPDX verification passed for all four platform manifests, and the package is public. All root and example workflows passed `actionlint`; `goreleaser check`, offline documentation link validation, `git diff --check`, and all seven Moon tasks passed.
